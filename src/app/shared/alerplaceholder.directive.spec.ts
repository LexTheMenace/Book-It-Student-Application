import { AlerplaceholderDirective } from './alerplaceholder.directive';

describe('AlerplaceholderDirective', () => {
  it('should create an instance', () => {
    const directive = new AlerplaceholderDirective();
    expect(directive).toBeTruthy();
  });
});
